{
  "database": {
    "file": "konsumdb"
  },
  "users": {
    "admin": {
      "password": "start123",
      "roles": ["admin"]
    }
  },
  "http": {
    "port": 12345,
    "_cert": "${document('cert.pem')}",
    "key": "${document('key.pem')}",
    "auth": {
      "jwt": {
        "expiresIn": "1h",
        "public": "${document('demo.rsa.pub')}",
        "private": "${document('demo.rsa')}"
      }
    }
  }
}
