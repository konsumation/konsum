{
  "database": {
    "file": "${statedir}/db"
  },
  "users": {
    "admin": {
      "password": "start123",
      "roles": [
        "admin"
      ]
    }
  },
  "http": {
    "port": 12345,
    "auth": {
      "jwt": {
        "options": {
          "expiresIn": "1h"
        },
        "public": "${document('demo.rsa.pub')}",
        "private": "${document('demo.rsa')}"
      }
    }
  }
}
